describe("widgetRouter", function() {


  beforeEach(function() {
  });

  it("should be able to get widgets from the db", function() {
    //player.play(song);
    //expect(player.currentlyPlayingSong).toEqual(song);

    //demonstrates use of custom matcher
    //expect(player).toBePlaying(song);
  });

  it("should be able to post widgets to the db", function() {

  });

});
